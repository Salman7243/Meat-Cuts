import 'package:flutter/material.dart';

class LeftoVER extends StatelessWidget {
  const LeftoVER({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}